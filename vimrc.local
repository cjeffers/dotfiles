UnBundle 'justinmk/vim-sneak'
color solarized
set bg=dark
set nospell

set guioptions-=r

"VIM + crontab
if $VIM_CRONTAB == "true"
    set nobackup
    set nowritebackup
endif

" omnifunc/NeoComplete is slow
setlocal omnifunc=
let g_neocomplete#enable_at_startup = 0
